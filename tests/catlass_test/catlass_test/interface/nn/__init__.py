import torch
from torch import nn
class ConvBias(nn.Module):
    """Test module for `examples/24_conv_bias`.
    This module is equal to `torch.nn.Conv3d`.
    """
    def __init__(
        self,
        in_channels: int,
        out_channels: int,
        kernel_size: _size_3_t,
        stride: _size_3_t = 1,
        padding: Union[str, _size_3_t] = 0,
        dilation: _size_3_t = 1,
        groups: int = 1,
        bias: bool = True,
        padding_mode: str = "zeros",
        device=None,
        dtype=None,
    ):