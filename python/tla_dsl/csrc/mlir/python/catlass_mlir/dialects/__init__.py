"""Dialect bindings for the CATLASS private MLIR package.

This package contains the upstream MLIR dialect wrappers needed by the frontend
(``arith``, ``memref`` and ``scf``), together with the generated TLA dialect
module. BiShengIR dialects remain C++-side only.
"""

from __future__ import annotations
