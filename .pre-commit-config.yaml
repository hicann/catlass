repos:
  - repo: local
    hooks:
      - id: oat-check
        name: OAT Compliance Check
        entry: ls
        language: system
        pass_filenames: true
        types: [file]
        stages: [commit]
        verbose: true
